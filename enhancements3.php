
<?php
/*	if($errMsg != ""){
		session_start();
		$_SESSION["errMsg"]= $errMsg;
		$_SESSION["firstname"]= $firstname;
		$_SESSION["lastname"]= $lastname;
		$_SESSION["Phone"]= $Phone;
		$_SESSION["pcontact"]= $pcontact;
		$_SESSION["street"]= $street;
		$_SESSION["town"]= $town;
		$_SESSION["pcode"]= $pcode;
		$_SESSION["state"]= $state;
		$_SESSION["cost"]= $cost;
		$_SESSION["email"]= $email;
		$_SESSION["quantity"]= $quantity;
*/
	?>	
		
<h1>SESSION</h1>
<ul>
<li><p>this function is really useful, in javascript we have sessionStorage but in php we use session_start() which is done the same job,(base on my understanding)d</p><br/></li>
<li><p>i found this function is really useful for the task , like this, where you have to pass data ,around.</p><br/></li>
<li><p>also, i place this code inside the process_order.php and fix_order.php for the pre populate the data in the box.</p><br/></li>
</ul>
<p><a href="http://php.net/manual/en/function.session-start.php">references</a></p>